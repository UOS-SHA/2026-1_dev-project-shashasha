# .env 는 깃에 올라가지 않음 (.gitignore 처리됨). 이 .example 만 공유용으로 올림.
#
# 로컬에서는 application.properties 의 기본값으로도 그냥 돌아가므로

DB_URL=jdbc:postgresql://localhost:5432/crew
DB_USERNAME=crew
DB_PASSWORD=crewpass

# 나중에 로그인(JWT) 붙일 때 사용할 비밀키 (아무에게도 공유 금지)
# JWT_SECRET=
